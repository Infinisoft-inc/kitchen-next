/*
 * Copyright © All rights reserved 2022
 * Infinisoft Inc.
 * www.infini-soft.com
 */

import { UseMutator } from "../types"

export const useMutator: UseMutator = (store, SK, field, newValue) => store.mutate(prev => {
  prev.list.set(SK, {
    ...item,
    [field]: newValue
  })

  return { ...prev }
})
