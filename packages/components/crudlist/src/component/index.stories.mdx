import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import  CrudList  from './index';
import { CrudListProps} from './types'

<Meta title="TEMPLATE/CrudList" component={ CrudList } />
export const Template = (args) => <CrudList {...args} />;

# CrudList

Insert descirption here.

<Canvas>
  <Story name="CrudList" args={ {flex: 1 } }>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="CrudList" />
